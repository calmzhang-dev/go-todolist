{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug main",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/cmd/myTodolist" // 替换了 "${fileDirname}" 为工作空间根目录
        }
    ]
}